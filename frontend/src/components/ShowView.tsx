import { useEffect  } from "react";

export const ShowView: React.FC<{}> = (): JSX.Element => {
    return (
        <div>   
            show comments and discussion goes here.
        </div>
    );
}